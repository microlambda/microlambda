import { deleteCustomDomain } from "./delete-custom-domain";

console.debug(deleteCustomDomain); // eslint-disable-line no-console

describe("[function] deleteCustomDomain", () => {
  it.todo("should do nothing if mapping do not exists");
  it.todo("should delete mapping if exists");
});
