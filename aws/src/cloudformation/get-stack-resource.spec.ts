import { getStackResource } from "./get-stack-resource";

console.debug(getStackResource); // eslint-disable-line no-console

describe("[function] getStackResource", () => {
  it.todo("should return stack resource if stack exists and resource exists");
  it.todo("should throw otherwise");
});
