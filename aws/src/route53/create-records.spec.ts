import { createLatencyRecord } from "./create-records";

console.debug(createLatencyRecord); // eslint-disable-line no-console

describe("[function] createLatencyRecord", () => {
  it.todo("should be tested");
});
