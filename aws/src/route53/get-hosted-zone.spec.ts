import { getHostedZone } from "./get-hosted-zone";

console.debug(getHostedZone); // eslint-disable-line no-console

describe("[function] createBasePathMapping", () => {
  it.todo("should be tested");
});
