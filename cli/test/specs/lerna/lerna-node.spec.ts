describe('The LernaNode class', () => {
  describe('The constructor', () => {
    test.todo('should build node metadata');
    test.todo('should build child node if not already built');
    test.todo('should reuse child node from built nodes array if already built');
  });
});
