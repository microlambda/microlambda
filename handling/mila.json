{
  "extends": "../mila.shared.json",
  "targets": {
    "test": {
      "cmd": "../node_modules/.bin/jest",
      "src": {
        "internals": [
          "{src,test}/**/*.spec.ts",
          "tsconfig.json",
          "jest.config.js"
        ],
        "deps": [
          "src/**/*.ts",
          "tsconfig.json"
        ]
      }
    }
  }
}
