{
  "extends": "../mila.shared.json",
  "targets": {
    "publish": {
      "cmd": "npm publish",
      "src": {
        "internals": ["lib/**/*.js", "dist/**/*.js", "package.json"],
        "deps": ["lib/**/*.js", "dist/**/*.js", "package.json"]
      }
    }
  }
}
