import { createCustomDomain } from "./create-custom-domain";

console.debug(createCustomDomain); // eslint-disable-line no-console

describe("[function] createBasePathMapping", () => {
  it.todo("should be tested");
});
