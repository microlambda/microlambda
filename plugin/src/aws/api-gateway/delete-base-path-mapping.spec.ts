import { deleteBasePathMapping } from "./delete-base-path-mapping";

console.debug(deleteBasePathMapping); // eslint-disable-line no-console

describe("[function] deleteBasePathMapping", () => {
  it.todo("should do nothing if mapping do not exists");
  it.todo("should delete mapping if exists");
});
