import { getBasePathMapping } from "./get-base-path-mapping";

console.debug(getBasePathMapping); // eslint-disable-line no-console

describe("[function] getBasePathMapping", () => {
  it.todo("should be tested");
});
