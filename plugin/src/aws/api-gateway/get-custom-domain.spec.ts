import { getCustomDomain } from "./get-custom-domain";

console.debug(getCustomDomain); // eslint-disable-line no-console

describe("[function] getCustomDomain", () => {
  it.todo("should be tested");
});
