import { getClosestCertificate } from "./get-closest-certificate";

console.debug(getClosestCertificate); // eslint-disable-line no-console

describe("[function] getClosestCertificate", () => {
  it.todo("should be tested");
});
