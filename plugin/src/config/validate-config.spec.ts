import { validateConfig } from "./validate-config";

console.debug(validateConfig); // eslint-disable-line no-console

describe("[function] validateConfig", () => {
  it.todo("should allow correct secrets configurations");
  it.todo("should not allow incorrect secrets configurations");
  it.todo("should allow correct domain configuration");
  it.todo("should not allow incorrect domain configuration");
});
