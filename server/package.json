{
  "name": "@microlambda/server",
  "version": "0.0.23-alpha",
  "description": "µlambda framework server",
  "author": "Mario Arnautou",
  "repository": {
    "url": "https://bitbucket.org/MarioArnt/microlambda/cli"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "test": "../node_modules/.bin/jest",
    "build": "yarn run pre:build && ../node_modules/.bin/tsc --build && yarn run post:build",
    "pre:build": "rm -rf ./dist",
    "post:build": "cp -r ../client/dist ./dist/static",
    "start": "node dist/index.js",
    "prettier": "../node_modules/.bin/prettier --write '{src,test}/**/*.{ts,tsx,json,yml}'",
    "lint": "../node_modules/.bin/eslint '{src,test}/**/*.{ts,tsx,json,yml}'"
  },
  "keywords": [
    "serverless",
    "typescript",
    "lerna"
  ],
  "dependencies": {
    "@microlambda/client": "workspace:*",
    "@microlambda/core": "workspace:*",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9"
  }
}
