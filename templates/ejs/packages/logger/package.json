{
  "name": "@dataportal/shared",
  "version": "0.3.6",
  "description": "Data Portal helpers that are shared between micro-services",
  "author": "Data Portal Development Team",
  "main": "lib/src/index.js",
  "typings": "lib/src/index.d.ts",
  "directories": {
    "lib": "lib"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "clear": "yarn run clear:modules && yarn run clear:lib && yarn run clear:package",
    "clear:modules": "rm -rf ./node_modules",
    "clear:lib": "rm -rf ./lib",
    "clear:package": "rm -rf ./.package",
    "build": "../../../node_modules/.bin/mila build -s @dataportal/shared",
    "tsc": "../../../node_modules/.bin/tsc -v &&  ../../../node_modules/.bin/tsc",
    "test": "yarn run test:unit",
    "test:unit": "../../../node_modules/.bin/dotenv -e ../../../.env.tests-ci -- ../../../node_modules/.bin/jest --config jest.config.unit.json --colors --runInBand --forceExit"
  },
  "dependencies": {
    "azure-ad-jwt-lite": "^1.1.0",
    "csv-parse": "^4.6.5",
    "got": "^11.1.3",
    "moment": "^2.24.0",
    "pino-pretty": "^4.3.0",
    "xss": "^1.0.7"
  },
  "devDependencies": {
    "@dataportal/types": "workspace:*"
  }
}
